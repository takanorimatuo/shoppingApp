#account-page.account-page
  .account-page__inner.clearfix
    .account-page__header
      会員情報登録
  .contents
    .field
      .field-label
        = f.label :nicname
      .field-input
        = f.text_field :nicname, autofocus: true
    .field
      .field-label
        = f.label :email
          .field-input
        = f.email_field :email
    .field
      .field-label
        = f.label :password
          %i (英数字8文字以上)
      .field-input
        = f.password_field :password, autocomplete: "off"